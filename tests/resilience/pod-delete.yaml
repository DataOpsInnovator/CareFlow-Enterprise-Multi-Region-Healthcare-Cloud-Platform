apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata: {name: patient-api-pod-failure, namespace: careflow}
spec: {action: pod-failure, mode: one, selector: {labelSelectors: {app: patient-api}}, duration: 60s}
