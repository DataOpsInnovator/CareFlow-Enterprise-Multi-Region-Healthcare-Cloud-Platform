# Security model

The repository follows least privilege, short-lived identity, encryption-by-default, immutable artifacts, policy-as-code, private control planes and auditable change. It assumes centralized organization controls for CloudTrail, GuardDuty, Security Hub, Config and incident telemetry. Synthetic healthcare payloads are used for portfolio demonstration; no PHI should ever be committed.
