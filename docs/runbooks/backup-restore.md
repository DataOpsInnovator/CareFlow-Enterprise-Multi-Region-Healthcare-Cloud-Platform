# Backup and restore runbook

Use only approved synthetic or production-safe test data for exercises. Confirm backup/PITR health, select the recovery point, restore into an isolated recovery target, validate schema and application integrity, run smoke tests, obtain change approval, then redirect clients only after data owners approve. Record start/end time, selected recovery point, measured RPO/RTO, validation evidence and cleanup actions.
