apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: patient-api-allow-dns
  namespace: patient-api
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: patient-api
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
