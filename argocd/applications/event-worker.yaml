apiVersion: argoproj.io/v1alpha1
kind: Application
metadata: {name: event-worker, namespace: argocd}
spec:
  project: careflow
  source: {repoURL: REPLACE_WITH_GIT_REPOSITORY_URL, targetRevision: main, path: helm/event-worker}
  destination: {server: https://kubernetes.default.svc, namespace: careflow}
  syncPolicy: {automated: {prune: true, selfHeal: true}, syncOptions: [CreateNamespace=true]}
