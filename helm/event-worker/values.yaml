replicaCount: 2
image:
  repository: REPLACE_WITH_ECR/event-worker
  tag: sha-REPLACE_WITH_GIT_SHA
resources:
  requests: {cpu: 100m, memory: 128Mi}
  limits: {cpu: 500m, memory: 512Mi}
service:
  port: 8080
